import styled from '@emotion/styled';

export const BaseButton = styled.button`
  margin: 0;
  border: none;
  padding: 0;
  background: none;

  &:focus {
    outline: none;
  }
  &:active {
    outline: none;
  }
`;
