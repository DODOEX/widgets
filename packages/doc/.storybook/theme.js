import { create } from 'storybook/theming';
import logo from './logo.svg';

export default create({
  base: 'light',
  brandTitle: 'dodo',
  brandUrl: 'https://app.dodoex.io/',
  brandImage: logo,
});