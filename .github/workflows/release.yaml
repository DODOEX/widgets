name: Release

on:
  workflow_dispatch: # manually
    inputs:
      dry_run:
        description: Skip publishing
        required: false
        default: true
        type: boolean


# on:
#   workflow_run:
#     workflows: ["Jest test"]
#     # branches: [ release/widgets ]
#     branches: [ feat/release-action ]
#     types: 
#       - completed


# on: push

jobs:
  release:
    runs-on: ubuntu-latest
    # if: ${{ always() && contains(join(needs.*.result, ','), 'Test success!') }}
    # needs: test
    steps:
      - name: Hello World          
        run: echo "Hello Release!"



# jobs:
#   test:
#     uses: ./.github/workflows/test.yml

#   release:
#     runs-on: ubuntu-latest
#     if: ${{ always() && contains(join(needs.*.result, ','), 'Test success!') }}
#     needs: test
#     steps:
#       - name: Hello World          
#         run: echo "Hello Release!"

  # release:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v3
  #     # - uses: ./.github/workflows/test

  #     - run: yarn test
  #       if: success()

  #     - run: yarn release --dry-run=${{ inputs.dry_run }}
  #       if: success()
  #       env:
  #         NPM_CONFIG_USERCONFIG: /dev/null
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #         NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
